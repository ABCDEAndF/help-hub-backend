package org.isolatedareas.helphub.domain;

/** How approved supplies reach the resident: collected at a service point or delivered by a mobile cart. */
public enum FulfillmentMethod {
    PICKUP,
    DELIVERY
}
